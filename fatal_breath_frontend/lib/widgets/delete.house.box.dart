import 'package:flutter/material.dart';

class DeleteHouseBox extends StatelessWidget {
  const DeleteHouseBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
