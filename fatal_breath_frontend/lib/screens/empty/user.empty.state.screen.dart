import 'package:flutter/material.dart';

class UserEmptyStateScreen extends StatelessWidget {
  const UserEmptyStateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
